{
  "name": "travel_fare_engine",
  "description": "Two-step travel fare quote pipeline: Validation/Pricing → JSON Formatting",
  "version": "1.0.0",
  "protocolVersion": "0.3.0",
  "url": "http://localhost:8081/",
  "preferredTransport": "JSONRPC",
  "additionalInterfaces": [
    { "url": "http://localhost:8081/", "transport": "JSONRPC" }
  ],
  "defaultInputModes": ["text/plain", "application/json"],
  "defaultOutputModes": ["application/json"],
  "capabilities": {
    "streaming": true
  },
  "skills": [
    {
      "id": "compute_fare",
      "name": "compute_fare",
      "description": "Compute a travel fare quote from a validated FareQuoteRequest.",
      "tags": ["travel", "pricing", "fare"],
      "inputSchema": {
        "type": "object",
        "properties": {
          "base_distance_miles": {
            "type": "integer",
            "minimum": 100,
            "maximum": 10000
          },
          "advance_purchase_days": {
            "type": "integer",
            "minimum": 0,
            "maximum": 365
          },
          "passengers": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "count": { "type": "integer", "minimum": 1, "maximum": 9 },
                "type": {
                  "type": "string",
                  "enum": ["adult", "child", "infant"]
                }
              },
              "required": ["count", "type"]
            }
          },
          "cabin_class": {
            "type": "string",
            "enum": ["economy", "premium_economy", "business", "first"]
          },
          "booking_class": {
            "type": "string",
            "enum": ["Y", "B", "M", "H", "Q", "G", "K"]
          },
          "route_type": {
            "type": "string",
            "enum": ["domestic", "international"]
          },
          "season_code": {
            "type": "string",
            "enum": ["low", "shoulder", "peak"]
          }
        },
        "required": [
          "base_distance_miles",
          "advance_purchase_days",
          "passengers",
          "cabin_class",
          "booking_class",
          "route_type",
          "season_code"
        ]
      }
    }
  ]
}
